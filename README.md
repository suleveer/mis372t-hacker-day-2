1. run in main directory:
npm install


2. run in main directory:
npm install @mui/material @emotion/react @emotion/styled @mui/icons-material


3. run in /server dir:
npm install mysql2


4. run in /server dir
node server.js


5. Open new terminal in main dir and run:
npm run dev
