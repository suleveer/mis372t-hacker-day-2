1. run in main directory:
npm install @mui/material @emotion/react @emotion/styled @mui/icons-material

2. run in /server dir:
npm install mysql2

3. run in /server dir
node server.js

4. Open new terminal in main dir and run:
npm run dev
